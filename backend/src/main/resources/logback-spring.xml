<?xml version="1.0" encoding="UTF-8"?>
<!--
  CRLF(로그 위조, CWE-117) 전역 방어 설정.
  로그 메시지(%m)에 들어 있는 개행 문자(\r, \n)를 공백으로 치환한다.
  호출 지점의 CrlfUtils.forLog() 와 함께 이중(2중)으로 막으며,
  이 설정은 우리 코드뿐 아니라 스프링/마이바티스 등 프레임워크 로그까지 모두 보호한다.
  참고: 파일명을 logback-spring.xml 로 두면 스프링부트가 이 설정을 자동으로 읽는다.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <!-- 핵심: %replace(%m){'개행', ' '} 로 메시지 안의 CR/LF 를 공백으로 바꾼다. -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>
    <property name="LOG_PATH" value="logs" />
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 매일 새로운 파일로 로그를 저장합니다. -->
            <fileNamePattern>${LOG_PATH}/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 지난 30일 간의 로그만 보관합니다. -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <!-- 파일에 저장될 때도 동일한 보안 포맷(CRLF 방어)을 적용합니다. -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- com.kiosk 패키지 하위에서 개발자가 직접 작성한 로그만 파일에 저장합니다. -->
    <!-- (프레임워크나 톰캣 등 외부 라이브러리 로그는 제외됨) -->
    <logger name="com.kiosk" level="INFO">
        <appender-ref ref="FILE"/>
    </logger>

    <!-- @Scheduled 작업에서 잡히지 않고 새어나온 예외는 스프링 기본 ErrorHandler가 이 로거로
         출력하는데, root에는 FILE이 없어 콘솔에만 남는다. 스케줄 장애도 파일에 남도록 연결한다. -->
    <logger name="org.springframework.scheduling.support" level="ERROR">
        <appender-ref ref="FILE"/>
    </logger>

</configuration>