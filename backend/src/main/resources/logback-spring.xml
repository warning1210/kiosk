<?xml version="1.0" encoding="UTF-8"?>
<!--
  CRLF(로그 위조, CWE-117) 전역 방어 설정.
  로그 메시지(%m)에 들어 있는 개행 문자(\r, \n)를 공백으로 치환한다.
  호출 지점의 CrlfUtils.forLog() 와 함께 이중(2중)으로 막으며,
  이 설정은 우리 코드뿐 아니라 스프링/마이바티스 등 프레임워크 로그까지 모두 보호한다.
  참고: 파일명을 logback-spring.xml 로 두면 스프링부트가 이 설정을 자동으로 읽는다.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <charset>UTF-8</charset>
            <!-- 핵심: %replace(%m){'개행', ' '} 로 메시지 안의 CR/LF 를 공백으로 바꾼다. -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>
    <property name="LOG_PATH" value="logs" />
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/application.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 매일 새로운 파일로 로그를 저장합니다. -->
            <fileNamePattern>${LOG_PATH}/application-%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 지난 30일 간의 로그만 보관합니다. -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <!-- 파일에 저장될 때도 동일한 보안 포맷(CRLF 방어)을 적용합니다. -->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>
    <!-- 시스템 메세지 로그 (요청, 응답) -->
    <appender name="SYSTEM_MSG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/system-msg.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/system-msg-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <!-- 접속정보 로그 (IP, URI, 지점명 등) -->
    <appender name="ACCESS_INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/access-info.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/access-info-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <!-- 지점 로그 (수정된 값 등) -->
    <appender name="BRANCH_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/branch.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/branch-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <!-- 본점 로그 (입력/수정된 값 등) -->
    <appender name="HQ_LOG_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/hq.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/hq-%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <charset>UTF-8</charset>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- com.kiosk 패키지 하위에서 개발자가 직접 작성한 로그만 파일에 저장합니다. -->
    <!-- (프레임워크나 톰캣 등 외부 라이브러리 로그는 제외됨) -->
    <logger name="com.kiosk" level="INFO">
        <appender-ref ref="FILE"/>
    </logger>

    <!-- 목적별 커스텀 로거 -->
    <!-- additivity="false"로 설정하여 루트 로거로 중복 출력되는 것을 방지합니다. -->
    <logger name="SYSTEM_MSG" level="INFO" additivity="false">
        <appender-ref ref="SYSTEM_MSG_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="ACCESS_INFO" level="INFO" additivity="false">
        <appender-ref ref="ACCESS_INFO_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="BRANCH_LOG" level="INFO" additivity="false">
        <appender-ref ref="BRANCH_LOG_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="HQ_LOG" level="INFO" additivity="false">
        <appender-ref ref="HQ_LOG_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

</configuration>