# 키오스크 배포 과정 정리 — GitHub → GitLab → EC2 자동 배포

> - **대상 서비스**: http://icekiosk.shop/ (→ 프론트엔드 EC2 공인 IP `3.34.173.143`)
> - **저장소**
>   - 백엔드: https://github.com/summitpsy2/kiosk-backend.git
>   - 프론트: https://github.com/summitpsy2/kiosk-frontend.git
> - **구조 한 줄 요약**: 서버 역할을 4개로 쪼갬 → **매니저(배포 지휘) + 프론트(화면) + 백엔드(API) + RDS(DB)**
> - **자동 배포 한 줄 요약**: `git push` 하면 → GitHub이 GitLab으로 미러링 → GitLab이 매니저 서버에 명령 → 매니저가 대상 서버에 코드 전송 후 도커 재빌드
> - **문서 작성일**: 2026-07-30

---

## 목차

1. [한눈에 보는 전체 그림](#1-한눈에-보는-전체-그림)
2. [인프라 구성 요약 (서버 4대 + 보안그룹)](#2-인프라-구성-요약-서버-4대--보안그룹)
3. [배포 흐름 — 단계별 요약](#3-배포-흐름--단계별-요약)
4. [최초 1회 수동 구축 과정 (간략)](#4-최초-1회-수동-구축-과정-간략)
5. [파일별 상세 해설 (★ 핵심)](#5-파일별-상세-해설--핵심)
   - [5-1. `mirror-to-gitlab.yml` — GitHub → GitLab 미러링](#5-1-mirror-to-gitlabyml--github--gitlab-미러링)
   - [5-2. 백엔드 `.gitlab-ci.yml` — 실제 배포 명령](#5-2-백엔드-gitlab-ciyml--실제-배포-명령)
   - [5-3. 프론트 `.gitlab-ci.yml` — 실제 배포 명령](#5-3-프론트-gitlab-ciyml--실제-배포-명령)
   - [5-4. 백엔드 `Dockerfile` — 스프링부트 이미지 만들기](#5-4-백엔드-dockerfile--스프링부트-이미지-만들기)
   - [5-5. 프론트 `Dockerfile` — Vue + Nginx 이미지 만들기](#5-5-프론트-dockerfile--vue--nginx-이미지-만들기)
   - [5-6. 프론트 `nginx.conf` — 화면 서빙 + API 프록시](#5-6-프론트-nginxconf--화면-서빙--api-프록시)
   - [5-7. `.dockerignore` — 이미지에 넣지 않을 파일](#5-7-dockerignore--이미지에-넣지-않을-파일)
   - [5-8. 서버 전용 파일: `docker-compose` + `.env`](#5-8-서버-전용-파일-docker-compose--env)
6. [파일들이 배포 단계에서 어떻게 이어지는가](#6-파일들이-배포-단계에서-어떻게-이어지는가)
7. [트러블슈팅 & 보안 체크리스트](#7-트러블슈팅--보안-체크리스트)
8. [현재 방식의 한계와 개선점](#8-현재-방식의-한계와-개선점)

---

## 1. 한눈에 보는 전체 그림

```
                                     인터넷
        ┌───────────────────────────────┴───────────────────────────────┐
   [관리자 PC]                                                      [일반 사용자]
    SSH:22 (내 IP만)                                          HTTP:80 (icekiosk.shop)
        │                                                             │
        ▼                                                             ▼
┌──────────────────────┐                                ┌──────────────────────────┐
│ ① 매니저 EC2          │      SSH:22 (사설망)             │ ② 프론트엔드 EC2          │
│   Bastion(관문 서버)   │───────────────────────────────▶│   nginx (컨테이너)        │
│   + GitLab Runner     │                                │   80 포트 🌐 외부공개     │
│   공인 3.34.192.103    │      SSH:22 (사설망)             │   공인 3.34.173.143     │
│   사설 172.31.12.209   │──────────────┐                 │   사설 172.31.11.138     │
└──────────────────────┘              │                 └────────────┬─────────────┘
                                       ▼                              │ /api, /uploads → :8080
                          ┌──────────────────────────┐               ▼
                          │ ③ 백엔드 EC2               │◀──────────────┘
                          │   Spring Boot (컨테이너)    │   8080 포트 🔒 프론트에게만
                          │   공인 13.124.88.110        │
                          │   사설 172.31.9.140         │
                          └────────────┬───────────────┘
                                       │ JDBC :3306
                                       ▼
                          ┌──────────────────────────┐
                          │ ④ RDS (MySQL 8.4) 관리형  │
                          │   3306 🔒 백엔드에게만     │
                          │   SSH 없음 · 백업/패치 자동 │
                          └──────────────────────────┘

   🌐 = 인터넷에 열림        🔒 = 특정 보안그룹에게만 열림
```

**한 문장 요약**: 사용자는 **프론트(80)** 로만 들어오고, 프론트는 **백엔드(8080)** 에게만, 백엔드는 **RDS(3306)** 에게만 접근합니다. 관리자는 **매니저 서버를 관문(Bastion)으로 거쳐야만** 프론트·백엔드에 SSH 할 수 있습니다. RDS는 관리형이라 **SSH 자체가 없고** 설치·백업·패치를 AWS가 대신합니다.

> 💡 `icekiosk.shop` 은 DNS A 레코드가 프론트 공인 IP(`3.34.173.143`)를 가리키도록 설정된 도메인입니다. nginx가 `server_name _;`(모든 호스트 허용)이라 도메인으로 들어와도 그대로 화면이 뜹니다. 현재는 HTTP만 사용(HTTPS/TLS 미구성).

---

## 2. 인프라 구성 요약 (서버 4대 + 보안그룹)

### 2-1. 서버 목록

| 역할 | 공인 IP | 사설 IP | 담당 |
|---|---|---|---|
| ① 매니저 EC2 | `3.34.192.103` | `172.31.12.209` | GitLab Runner 실행, 소스 전달, 원격 배포 명령 |
| ② 프론트 EC2 | `3.34.173.143` | `172.31.11.138` | Vue 정적 화면 제공, `/api`·`/uploads`를 백엔드로 프록시 |
| ③ 백엔드 EC2 | `13.124.88.110` | `172.31.9.140` | Spring Boot API 실행, RDS 연결, 업로드 저장 |
| ④ RDS MySQL | (공인 없음) | 엔드포인트로 접속 | 데이터 저장 (`kiosk-rds.c7i0wus8a07b.ap-northeast-2.rds.amazonaws.com`) |

- **리전**: 서울(`ap-northeast-2`) / **VPC**: `172.31.0.0/16` / **키페어**: `kiosk-seoul-key`
- **RDS 사양**: MySQL 8.4, `db.t4g.micro`, gp3 20GiB, 단일 AZ, 퍼블릭 액세스 **아니요**, 파라미터그룹으로 `utf8mb4` + `Asia/Seoul` 지정

> ⚠️ 매니저·백엔드 공인 IP는 탄력적 IP(고정)가 아니라서 인스턴스를 중지 후 재시작하면 바뀔 수 있습니다. 프론트만 탄력적 IP로 `3.34.173.143` 고정.

### 2-2. 보안 그룹 (격리의 핵심)

각 서버 앞단 방화벽입니다. "**어떤 포트를 누구에게 열지**"를 IP가 아니라 **다른 보안그룹을 지목**해서 정의합니다.

| 보안 그룹 | 인바운드 규칙 | 의미 |
|---|---|---|
| `kiosk-sg-manager` | SSH 22 ← 관리자 PC 공인 IP | 관리자만 매니저에 접속 |
| `kiosk-sg-frontend` | HTTP 80 ← `0.0.0.0/0`, SSH 22 ← `kiosk-sg-manager` | 화면은 전체 공개, SSH는 매니저 경유만 |
| `kiosk-sg-backend` | TCP 8080 ← `kiosk-sg-frontend`, SSH 22 ← `kiosk-sg-manager` | API는 프론트에게만, SSH는 매니저 경유만 |
| `kiosk-sg-rds` | MySQL 3306 ← `kiosk-sg-backend` | DB는 백엔드에게만 (SSH 규칙 자체가 없음) |

---

## 3. 배포 흐름 — 단계별 요약

**전체를 한 줄로:**

```
git push → GitHub Actions가 GitLab으로 미러링 → GitLab 파이프라인 시작
       → 매니저의 GitLab Runner가 Job 실행 → rsync로 대상 EC2에 소스 전송
       → SSH로 대상 EC2에서 docker build → docker compose로 컨테이너 교체 → 헬스체크
```

**단계별로:**

| 단계 | 어디서 | 무슨 일이 | 담당 파일 |
|---|---|---|---|
| 1 | 내 PC | 코드 수정 후 `git push origin main` | (개발자) |
| 2 | GitHub | `main` push 감지 → GitLab `main`으로 강제 미러링 | `.github/workflows/mirror-to-gitlab.yml` |
| 3 | GitLab | 미러링 push를 받아 파이프라인(deploy) 시작 | `.gitlab-ci.yml` |
| 4 | 매니저 EC2 | Runner가 Job을 잡고 대상 EC2로 소스 rsync + SSH 접속 | `.gitlab-ci.yml` |
| 5 | 대상 EC2 | 전송된 소스로 `docker build` | `Dockerfile` |
| 6 | 대상 EC2 | `docker compose`로 컨테이너 교체(재생성) | `docker-compose.*.yml` + `.env` |
| 7 | 대상 EC2 | `curl`로 자동 헬스체크, 실패 시 로그 출력 & Job 실패 | `.gitlab-ci.yml` |

> 핵심: **이미지는 매니저가 아니라 각 대상 EC2에서 직접 빌드**됩니다. 그래서 Docker Registry(이미지 저장소)가 필요 없습니다. 매니저는 "지휘"만 합니다.

---

## 4. 최초 1회 수동 구축 과정 (간략)

자동 배포가 돌기 전에, 사람이 손으로 딱 한 번 만든 것들입니다.

```
1. VPC·서브넷·보안그룹 4개 구성
2. 매니저·프론트·백엔드 EC2 3대 생성 (키페어 kiosk-seoul-key)
3. 프론트에 탄력적 IP(3.34.173.143) 연결 + icekiosk.shop DNS 연결
4. RDS(MySQL 8.4) 생성 + 서브넷그룹 + 파라미터그룹(utf8mb4, Asia/Seoul)
5. 내 PC ~/.ssh/config 에 ProxyJump 설정 (매니저 경유 접속)
6. EC2 3대에 Docker/rsync/git/swap 설치, 백엔드엔 mysql-client 추가
7. 기존 DB 덤프(kiosk-full-dump.sql)를 RDS로 복원 + 앱 전용 계정 kiosk 생성
8. 백엔드 EC2: git clone → .env / docker-compose.backend.yml 작성 → 최초 build & up
9. 프론트 EC2: git clone → .env(BACKEND_HOST) / docker-compose.frontend.yml 작성 → 최초 build & up
10. 매니저에 GitLab Runner 설치 + Runner 전용 SSH 키 생성
    → 공개키를 프론트·백엔드 authorized_keys에 등록
11. GitLab Project Runner 2개 등록(tag: manager) + CI/CD 변수 등록
12. GitHub Secret(GITLAB_TOKEN) 등록 + 미러링 워크플로 배치
```

> 이 중 `.env`, `docker-compose.*.yml` 은 **서버(EC2)에만 존재**하고 Git 저장소에는 커밋하지 않습니다(비밀값 보호). 그래서 "저장소만 clone하면 100% 복구"되지는 않습니다.

---

## 5. 파일별 상세 해설 (★ 핵심)

> 아래 코드는 **실제 저장소의 파일 내용**에, 이해를 돕는 **한 줄 한 줄 주석**을 붙인 설명본입니다.
> `.gitlab-ci.yml` 의 `>`(folded scalar) 블록은 원칙상 중간에 주석을 넣으면 안 되므로(주석이 다음 명령까지 삼켜버림), 그 부분은 **표로 한 줄씩 해설**합니다. 원본에 그대로 덮어쓰지 마세요.

---

### 5-1. `mirror-to-gitlab.yml` — GitHub → GitLab 미러링

📍 **위치**: `백엔드/.github/workflows/`, `프론트/.github/workflows/` (두 저장소 각각 존재)
📍 **역할**: 배포 흐름 **2단계**. 개발자가 GitHub `main`에 push하면, 이 파일이 GitHub Actions에서 실행되어 코드를 **GitLab으로 그대로 복사(미러링)** 합니다. GitLab이 실제 배포를 돌리기 때문에, 이 파일이 "GitHub → GitLab 다리" 역할을 합니다.

**백엔드 버전** (프론트는 마지막 URL만 `kiosk-frontend`로 다름):

```yaml
# 이 워크플로의 이름 (GitHub Actions 탭에 표시됨)
name: Mirror to GitLab

# 언제 실행할지(트리거) 정의
on:
  push:                    # 누군가 push 했을 때
    branches: [main]       # 그중에서도 main 브랜치에 push 됐을 때만

# 실행할 작업(Job) 목록
jobs:
  mirror:                  # 'mirror'라는 이름의 Job 하나
    runs-on: ubuntu-latest # GitHub가 빌려주는 임시 우분투 가상머신에서 실행

    steps:                 # Job이 순서대로 밟는 단계들
      # 1단계: GitHub 저장소 코드를 임시 우분투로 내려받음(checkout)
      - uses: actions/checkout@v4
        with: { fetch-depth: 0 }   # 최근 1개가 아니라 "전체 커밋 이력"을 통째로 가져옴
                                    # (미러링은 이력 전체를 밀어야 하므로 반드시 0)

      # 2단계: 내려받은 코드를 GitLab으로 밀어넣기
      - name: Push to GitLab        # 이 단계의 표시 이름
        run: |                      # 아래 셸 명령을 실행
          # GitHub의 현재 HEAD(방금 push된 최신 커밋)를 GitLab main으로 강제 push
          # oauth2:토큰 형태로 인증하며, 토큰은 GitHub Secret에 저장된 값을 사용(파일엔 안 남음)
          git push --force https://oauth2:${{ secrets.GITLAB_TOKEN }}@gitlab.com/summitpsy/kiosk-backend.git HEAD:main
```

**꼭 알아둘 점**
- `--force`(강제 push)라서 **GitLab을 GitHub과 무조건 똑같이** 맞춥니다. 즉 GitLab에서 직접 수정한 커밋은 다음 미러링 때 덮어써져 사라집니다 → **코드 수정은 항상 GitHub에서** 하세요.
- `${{ secrets.GITLAB_TOKEN }}` 은 GitHub 저장소 **Settings → Secrets → Actions** 에 등록한 GitLab 접근 토큰입니다. 파일에는 이름만 있고 실제 값은 없습니다.
- 백엔드·프론트 **두 GitHub 저장소 각각** `GITLAB_TOKEN` Secret이 있어야 합니다.

---

### 5-2. 백엔드 `.gitlab-ci.yml` — 실제 배포 명령

📍 **위치**: `백엔드` 저장소 루트
📍 **역할**: 배포 흐름 **3~7단계**. GitLab이 이 파일을 읽어 파이프라인을 만들고, 매니저 EC2의 GitLab Runner가 **백엔드 EC2로 소스를 보내고(rsync) → 그 서버에서 도커 이미지를 빌드 → 컨테이너를 교체 → 헬스체크**까지 수행합니다.

**실제 파일 (구조 부분엔 인라인 주석, `>` 블록은 아래 표로 해설):**

```yaml
# 파이프라인의 단계(stage) 목록. 지금은 deploy 하나만 씀
stages:
  - deploy

# 'deploy-backend'라는 Job 정의
deploy-backend:
  stage: deploy            # 위에서 선언한 deploy 단계에 속함

  tags:                    # 이 Job을 어떤 Runner가 잡을지 태그로 지정
    - manager              # 'manager' 태그를 가진 Runner(=매니저 EC2)만 실행 가능

  # 같은 배포가 동시에 겹쳐 서버를 망가뜨리지 않도록, 이 그룹은 한 번에 하나씩만 실행
  resource_group: kiosk-backend-production

  rules:                   # 이 Job을 실행할 조건
    # 현재 브랜치가 GitLab 기본 브랜치(main)일 때만 실행
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'

  variables:               # 이 Job 안에서 쓸 변수들
    # 매니저 EC2에 있는 Runner 전용 SSH 개인키 경로
    SSH_KEY: /home/gitlab-runner/.ssh/id_ed25519
    # 접속 대상 = ubuntu@<백엔드 사설 IP>. BACKEND_PRIVATE_IP는 GitLab CI/CD 변수에서 주입됨
    TARGET: ubuntu@$BACKEND_PRIVATE_IP

  before_script:           # 본 배포 전에 "안전장치"로 미리 검사
    - test -n "$BACKEND_PRIVATE_IP"   # 대상 IP 변수가 비어있으면 즉시 실패(엉뚱한 곳 배포 방지)
    - test -r "$SSH_KEY"              # SSH 키를 읽을 수 없으면 즉시 실패

  script:                  # 실제 배포 명령 (아래 두 개의 '>' 블록)
    # [블록 A] 소스 전송: 여러 줄이 공백으로 이어져 하나의 rsync 명령이 됨
    - >
      rsync -az --delete
      --exclude='.git'
      -e "ssh -i $SSH_KEY -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes"
      ./
      "$TARGET:/opt/kiosk/src-backend/"
    # [블록 B] 원격 실행: SSH로 백엔드 EC2에 접속해 빌드·교체·헬스체크
    - >
      ssh -i "$SSH_KEY"
      -o IdentitiesOnly=yes
      -o StrictHostKeyChecking=yes
      "$TARGET"
      'set -e;
      cd /opt/kiosk;
      docker build -t kiosk-backend:latest ./src-backend;
      docker compose -f docker-compose.backend.yml --env-file .env up -d --force-recreate backend;
      for i in $(seq 1 60); do
        if curl -fsS http://127.0.0.1:8080/api/categories >/dev/null; then
          echo "Backend health check OK";
          docker image prune -f;
          exit 0;
        fi;
        sleep 2;
      done;
      echo "Backend health check failed";
      docker logs --tail 150 kiosk-backend;
      exit 1'
```

> ⚠️ `>` 블록 안에 `#` 주석을 넣으면 안 됩니다. YAML이 줄바꿈을 공백으로 이어붙이기 때문에, 주석 하나가 뒤따르는 명령까지 통째로 주석 처리해 배포가 안 됩니다. 그래서 아래 표로 대신 해설합니다.

**[블록 A] rsync — 소스 전송 한 줄씩:**

| 코드 | 뜻 |
|---|---|
| `rsync -az --delete` | 파일 속성 보존(a)·압축(z) 전송하고, 원본에 없는 대상 파일은 삭제(--delete)해 완전 동기화 |
| `--exclude='.git'` | `.git` 폴더는 전송·삭제 대상에서 제외 |
| `-e "ssh -i $SSH_KEY -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes"` | 전송 통로로 SSH 사용. 지정한 키만 쓰고(IdentitiesOnly), 등록된 호스트키와 다르면 거부(Strict) |
| `./` | "현재 폴더 전체"(Runner가 checkout한 최신 소스)를 원본으로 |
| `"$TARGET:/opt/kiosk/src-backend/"` | 백엔드 EC2의 `/opt/kiosk/src-backend/`를 목적지로 |

**[블록 B] SSH 원격 실행 — 한 줄씩:**

| 코드 | 뜻 |
|---|---|
| `ssh -i "$SSH_KEY" -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes "$TARGET"` | Runner 전용 키로 백엔드 EC2에 SSH 접속 |
| `'set -e;` | 이후 원격 명령 중 하나라도 실패하면 즉시 중단 |
| `cd /opt/kiosk;` | Compose와 `.env`가 있는 배포 폴더로 이동 |
| `docker build -t kiosk-backend:latest ./src-backend;` | 방금 받은 소스로 `kiosk-backend:latest` 이미지 새로 빌드 |
| `docker compose -f docker-compose.backend.yml --env-file .env up -d --force-recreate backend;` | 새 이미지 + 서버 `.env`로 백엔드 컨테이너를 강제 재생성 |
| `for i in $(seq 1 60); do` | 아래 헬스체크를 최대 60번 반복 |
| `if curl -fsS http://127.0.0.1:8080/api/categories >/dev/null; then` | 서버 내부에서 API가 정상 응답하는지 확인 |
| `echo "Backend health check OK";` | 성공 메시지 출력 |
| `docker image prune -f;` | 안 쓰는(dangling) 이미지 정리 |
| `exit 0;` | 성공으로 종료 |
| `fi; sleep 2; done;` | 아직 준비 안 됐으면 2초 대기 후 반복 |
| `echo "Backend health check failed";` | 60번(약 2분) 안에 못 뜨면 실패 메시지 |
| `docker logs --tail 150 kiosk-backend;` | 원인 파악용 최근 로그 150줄 출력 |
| `exit 1'` | 실패로 종료(파이프라인 빨간불) |

---

### 5-3. 프론트 `.gitlab-ci.yml` — 실제 배포 명령

📍 **위치**: `프론트` 저장소 루트
📍 **역할**: 백엔드 버전과 구조가 같습니다. 다른 점만 짚으면 ①대상이 프론트 EC2 ②rsync에서 `node_modules`·`dist`도 제외(서버에서 새로 빌드하므로) ③헬스체크를 3종류(화면·API 프록시·SPA 경로)로 함 ④`environment`(배포 대상 링크) 블록이 추가됨.

```yaml
stages:
  - deploy                 # 배포 단계 하나

deploy-frontend:           # 'deploy-frontend' Job
  stage: deploy
  tags:
    - manager              # 매니저 EC2 Runner만 실행

  resource_group: kiosk-frontend-production   # 프론트 배포도 한 번에 하나씩

  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'   # main일 때만

  variables:
    SSH_KEY: /home/gitlab-runner/.ssh/id_ed25519      # Runner 전용 SSH 키
    TARGET: ubuntu@$FRONTEND_PRIVATE_IP               # 대상 = 프론트 EC2 사설 IP

  before_script:
    - test -n "$FRONTEND_PRIVATE_IP"   # 대상 IP 비었으면 실패
    - test -r "$SSH_KEY"               # 키 못 읽으면 실패

  script:
    # [블록 A] 소스 전송 (백엔드와 달리 node_modules·dist 제외)
    - >
      rsync -az --delete
      --exclude='.git'
      --exclude='node_modules'
      --exclude='dist'
      -e "ssh -i $SSH_KEY -o IdentitiesOnly=yes -o StrictHostKeyChecking=yes"
      ./
      "$TARGET:/opt/kiosk/src-frontend/"
    # [블록 B] 원격 빌드·교체·헬스체크
    - >
      ssh -i "$SSH_KEY"
      -o IdentitiesOnly=yes
      -o StrictHostKeyChecking=yes
      "$TARGET"
      'set -e;
      cd /opt/kiosk;
      docker build -t kiosk-frontend:latest ./src-frontend;
      docker compose -f docker-compose.frontend.yml --env-file .env up -d --force-recreate frontend;
      for i in $(seq 1 30); do
        if curl -fsS http://127.0.0.1/ >/dev/null &&
           curl -fsS http://127.0.0.1/api/categories >/dev/null &&
           curl -fsS http://127.0.0.1/branch/login >/dev/null; then
          echo "Frontend and API health checks OK";
          docker image prune -f;
          exit 0;
        fi;
        sleep 2;
      done;
      echo "Frontend health check failed";
      docker logs --tail 150 kiosk-frontend;
      exit 1'

  # GitLab의 Environment(배포 이력) 화면에 표시될 정보
  environment:
    name: production                 # 환경 이름
    url: http://3.34.173.143         # GitLab 화면에서 "바로가기" 링크 (실제 네트워크 설정 아님)
```

**[블록 A] rsync 제외 항목이 다른 이유**

| 코드 | 뜻 |
|---|---|
| `--exclude='node_modules'` | 내 PC의 무거운 의존성 폴더는 안 보냄 (서버가 `npm ci`로 새로 설치) |
| `--exclude='dist'` | 로컬 빌드 결과물도 안 보냄 (서버가 `npm run build`로 새로 만듦) |

**[블록 B] 헬스체크가 3종류인 이유**

| 코드 | 뜻 |
|---|---|
| `curl http://127.0.0.1/` | nginx가 첫 화면(index.html)을 잘 주는지 |
| `curl http://127.0.0.1/api/categories` | nginx→백엔드 API 프록시가 잘 이어지는지 |
| `curl http://127.0.0.1/branch/login` | Vue Router 경로 새로고침(SPA fallback)이 잘 되는지 |
| 나머지 `for/sleep/echo/prune/exit` 부분 | 백엔드 버전과 동일 (최대 30회 = 약 1분 대기) |

---

### 5-4. 백엔드 `Dockerfile` — 스프링부트 이미지 만들기

📍 **위치**: `백엔드` 저장소 루트
📍 **역할**: 배포 흐름 **5단계**. `.gitlab-ci.yml`의 `docker build`가 이 파일을 읽어 **Spring Boot 실행 이미지**를 만듭니다. "빌드 단계(Maven)"와 "실행 단계(JRE만)"를 나눈 **멀티스테이지** 방식이라 최종 이미지가 가볍습니다.

```dockerfile
# ── ① 빌드 단계: 소스를 JAR로 만드는 임시 작업장 ──
# Maven 3.9 + Java 21 JDK가 깔린 이미지를 'build'라는 별명으로 사용
FROM maven:3.9-eclipse-temurin-21 AS build

# 컨테이너 안에서 명령을 실행할 기준 폴더
WORKDIR /app

# 의존성 목록 파일(pom.xml)을 소스보다 "먼저" 복사
COPY pom.xml .

# pom.xml에 적힌 라이브러리를 미리 다운로드해 레이어로 저장
# → pom.xml이 안 바뀌면 소스만 바뀌어도 이 다운로드 레이어를 재사용(빌드 빨라짐)
RUN mvn -B dependency:go-offline

# 실제 자바 소스 코드를 복사
COPY src ./src

# 테스트는 건너뛰고(-DskipTests) 실행 가능한 JAR를 패키징
RUN mvn -B -DskipTests package

# ── ② 실행 단계: 진짜 서비스용 가벼운 이미지 ──
# Maven·JDK 없이 Java 21 "실행 전용(JRE)" 이미지만 사용
FROM eclipse-temurin:21-jre-jammy

# 실행 컨테이너의 작업 폴더
WORKDIR /app

# ①단계에서 만든 JAR 하나만 가져와 app.jar로 저장 (소스·Maven 캐시는 안 가져옴)
COPY --from=build /app/target/*.jar app.jar

# 이 이미지는 컨테이너 내부 8080 포트를 쓴다는 "표시"(문서용 메타데이터)
# 실제 포트 연결은 docker-compose의 ports가 담당
EXPOSE 8080

# 컨테이너가 켜지면 실행할 고정 명령: JAR를 자바로 구동
# DB_HOST 등 접속정보는 여기 없고, compose가 .env로 환경변수 주입
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**핵심 이해 포인트**
- **왜 2단계로 나누나?** 1단계에는 Maven·JDK·소스가 다 필요하지만, 실제 서비스에는 JRE + JAR만 있으면 됩니다. 최종 이미지에서 무거운 것들을 빼서 **작고 안전한 이미지**를 만듭니다.
- `pom.xml`을 소스보다 먼저 복사하는 이유는 **캐시** 때문입니다(의존성이 안 바뀌면 재다운로드 생략).

---

### 5-5. 프론트 `Dockerfile` — Vue + Nginx 이미지 만들기

📍 **위치**: `프론트` 저장소 루트
📍 **역할**: 배포 흐름 **5단계**. `docker build`가 이 파일로 **Nginx 실행 이미지**를 만듭니다. "Node로 Vue를 빌드"한 뒤, 결과물(dist)만 Nginx 이미지로 옮기는 멀티스테이지입니다.

```dockerfile
# ── ① 빌드 단계: Vite로 dist(정적파일) 생성 ──
# Node.js 22 이미지를 'build'라는 별명으로 사용
FROM node:22 AS build

# 작업 기준 폴더
WORKDIR /app

# 의존성 목록(package.json, package-lock.json)을 먼저 복사
# → 소스만 바뀌면 아래 npm 설치 레이어를 재사용(빌드 빨라짐)
COPY package*.json ./

# package-lock.json 기준으로 정확히 같은 버전 설치(재현성 보장)
RUN npm ci

# .dockerignore에 없는 나머지 프로젝트 소스 전체 복사
COPY . .

# Vite 빌드 실행 → 결과물이 /app/dist 에 생성됨(HTML/CSS/JS)
RUN npm run build

# ── ② 실행 단계: nginx로 dist 서빙 + /api 프록시 ──
# 가벼운 Nginx Alpine 이미지 사용
FROM nginx:alpine

# ①단계에서 만든 dist 정적파일만 Nginx 웹 루트로 복사(Node·소스는 안 가져옴)
COPY --from=build /app/dist /usr/share/nginx/html

# nginx.conf를 "환경변수 치환 템플릿" 위치로 복사
# → 컨테이너 시작 시 nginx가 ${BACKEND_HOST}를 실제 IP로 바꿔서 적용
COPY nginx.conf /etc/nginx/templates/default.conf.template

# 컨테이너 내부 80 포트 사용 표시(문서용). 실제 연결은 compose의 ports가 담당
EXPOSE 80
```

**핵심 이해 포인트**
- 최종 이미지에는 **Node·npm·소스가 없습니다.** Nginx가 완성된 HTML/CSS/JS만 서빙합니다.
- `nginx.conf`를 `/etc/nginx/templates/...template` 위치에 두는 게 중요합니다. Nginx 공식 이미지는 이 폴더의 `.template` 파일을 **시작할 때 `envsubst`로 자동 치환**해줍니다(다음 5-6 참고).

---

### 5-6. 프론트 `nginx.conf` — 화면 서빙 + API 프록시

📍 **위치**: `프론트` 저장소 루트
📍 **역할**: 프론트 컨테이너의 웹서버 규칙. **정적 화면을 제공**하면서 `/api`·`/uploads` 요청만 **백엔드 EC2로 넘기고(프록시)**, Vue Router 새로고침을 처리합니다.

```nginx
# ─────────────────────────────────────────────────────────────
# ${BACKEND_HOST} 는 컨테이너가 켜질 때 백엔드 EC2의 "사설 IP"로 치환됩니다.
# nginx 공식 이미지가 /etc/nginx/templates/*.template 를 시작 시 envsubst로
# 자동 치환해 conf.d/ 에 써주므로, 별도 스크립트가 필요 없습니다.
# (주의: $host, $remote_addr 등 nginx 자체 변수는 환경변수가 아니므로 그대로 보존됩니다)
# ─────────────────────────────────────────────────────────────
server {
  listen 80;                       # 컨테이너 80 포트에서 HTTP 요청 수신
  server_name _;                   # 특정 도메인 지정 없이 모든 Host 요청 처리(icekiosk.shop도 여기로)
  root /usr/share/nginx/html;      # Vue 빌드 결과(dist)가 놓인 웹 루트
  index index.html;                # 기본 문서

  # API 요청 → 백엔드 EC2:8080 으로 전달
  location /api/ {                                   # 경로가 /api/ 로 시작하면
    proxy_pass http://${BACKEND_HOST}:8080;          # 백엔드 EC2 8080으로 그대로 넘김
    proxy_set_header Host $host;                      # 사용자가 요청한 원래 Host 헤더 전달
    proxy_set_header X-Real-IP $remote_addr;          # 실제 접속자 IP 전달
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;  # 거쳐온 IP 목록 누적 전달
    proxy_set_header X-Forwarded-Proto $scheme;       # 원 요청이 http/https 였는지 전달
  }

  # 업로드 파일(이미지 등) → 백엔드 EC2:8080 으로 전달
  location /uploads/ {                               # 경로가 /uploads/ 로 시작하면
    proxy_pass http://${BACKEND_HOST}:8080;          # 백엔드 8080에서 파일 가져옴
    proxy_set_header Host $host;                      # 원 Host 헤더 전달
  }

  # SPA(Vue Router history 모드): 새로고침해도 index.html로 fallback
  location / {                                       # 위 규칙에 안 걸린 나머지 요청
    try_files $uri $uri/ /index.html;                # 실제 파일→폴더→없으면 index.html 반환
  }                                                  # → Vue Router가 화면 전환 담당
}
```

**핵심 이해 포인트**
- 브라우저는 백엔드 IP를 전혀 몰라도 됩니다. 같은 프론트 주소로 `/api`를 부르면 nginx가 **내부망에서만** 백엔드로 넘깁니다(격리 유지).
- `${BACKEND_HOST}` 는 프론트 EC2의 `/opt/kiosk/.env` → compose → 컨테이너 환경변수 → envsubst 순서로 **실제 사설 IP(`172.31.9.140`)** 로 바뀝니다.

**`${BACKEND_HOST}` 치환 흐름:**

```
프론트 EC2 /opt/kiosk/.env  (BACKEND_HOST=172.31.9.140)
   → docker-compose.frontend.yml 의 environment
   → nginx 컨테이너 환경변수
   → /etc/nginx/templates/default.conf.template
   → (nginx가 시작 시 envsubst 치환)
   → /etc/nginx/conf.d/default.conf
   → proxy_pass http://172.31.9.140:8080
```

---

### 5-7. `.dockerignore` — 이미지에 넣지 않을 파일

📍 **역할**: `docker build` 할 때 이미지 안으로 **복사하지 말 것** 목록. 빌드 속도를 높이고 이미지를 깨끗하게 유지합니다.

**백엔드 `.dockerignore`:**
```gitignore
target          # Maven 빌드 산출물 (이미지 안에서 새로 빌드하므로 제외)
.idea           # IntelliJ 설정 폴더
*.iml           # IntelliJ 모듈 파일
.vscode         # VS Code 설정 폴더
.classpath      # 이클립스 클래스패스 설정
.factorypath    # 이클립스 애노테이션 프로세서 설정
.settings       # 이클립스 프로젝트 설정 폴더
```

**프론트 `.dockerignore`:**
```gitignore
node_modules    # 로컬 의존성 (이미지 안에서 npm ci로 새로 설치)
dist            # 로컬 빌드 결과 (이미지 안에서 새로 빌드)
.git            # Git 이력 (실행에 불필요)
*.log           # 각종 로그 파일
.env            # 비밀값 환경변수 (이미지에 절대 넣지 않음)
.env.local      # 로컬 전용 환경변수
```

---

### 5-8. 서버 전용 파일: `docker-compose` + `.env`

📍 **중요**: 이 파일들은 **Git 저장소에 없고, 각 EC2의 `/opt/kiosk/` 에만** 존재합니다(비밀값 보호). 배포 시 `rsync`는 `src-backend`/`src-frontend`만 덮어쓰므로 이 파일들과 `uploads/`는 안전하게 유지됩니다.

#### 백엔드 `/opt/kiosk/docker-compose.backend.yml`

```yaml
services:                          # 실행할 컨테이너 서비스 목록
  backend:                         # 서비스 이름(반드시 .gitlab-ci.yml의 'backend'와 일치)
    image: kiosk-backend:latest    # 이 서버에서 방금 빌드한 로컬 이미지 사용
    container_name: kiosk-backend  # docker ps·logs에서 보일 컨테이너 이름 고정
    restart: unless-stopped        # 서버 재부팅/장애 시 자동 재시작(사용자가 stop한 경우 제외)
    init: true                     # 좀비 프로세스 정리용 init을 PID 1로
    env_file:
      - .env                       # 같은 폴더의 .env 값을 컨테이너 환경변수로 주입
    environment:                   # .env 외 추가 환경변수
      TZ: Asia/Seoul               # 컨테이너 시간대
      JAVA_TOOL_OPTIONS: "-Xms128m -Xmx512m -Duser.timezone=Asia/Seoul"  # JVM 메모리·시간대
    ports:
      - "8080:8080"                # EC2 8080 → 컨테이너 8080 (외부 차단은 보안그룹이 담당)
    volumes:
      - ./uploads:/app/uploads     # 업로드 파일을 EC2에 보존(컨테이너 교체해도 안 사라짐)
    logging:
      driver: json-file            # 기본 JSON 파일 로그
      options:
        max-size: "10m"            # 로그 파일 하나 최대 10MB
        max-file: "3"              # 최대 3개까지 보관(용량 폭주 방지)
```

#### 백엔드 `/opt/kiosk/.env` (값은 예시, 실제 비밀값은 서버에만)

```dotenv
DB_HOST=kiosk-rds.c7i0wus8a07b.ap-northeast-2.rds.amazonaws.com  # RDS 엔드포인트(주소)
DB_PORT=3306                        # MySQL 포트
DB_NAME=kiosk                       # 사용할 DB 이름
DB_USERNAME=kiosk                   # 앱 전용 계정(최소권한: SELECT/INSERT/UPDATE/DELETE만)
DB_PASSWORD=<앱DB비밀번호>            # 실제 값은 Git에 커밋하지 않음
APP_FRONTEND_URL=http://3.34.173.143 # 이메일 링크·결제 복귀 등의 기준 주소
MAIL_ENABLED=false                  # 메일 발송 비활성화
FIREBASE_CREDENTIALS_PATH=          # Firebase 미사용(빈 값)
HQ_TOKEN_SECRET=<랜덤64자>           # 본사 관리자 토큰 서명 비밀키
SPRING_JPA_HIBERNATE_DDL_AUTO=validate  # 스키마를 건드리지 않고 현재 스키마만 검증
```

#### 프론트 `/opt/kiosk/docker-compose.frontend.yml`

```yaml
services:
  frontend:                        # 서비스 이름(.gitlab-ci.yml의 'frontend'와 일치)
    image: kiosk-frontend:latest   # 이 서버에서 빌드한 로컬 이미지
    container_name: kiosk-frontend # 컨테이너 이름 고정
    restart: unless-stopped        # 자동 재시작
    init: true                     # init을 PID 1로
    environment:
      BACKEND_HOST: ${BACKEND_HOST}  # .env의 값을 nginx로 전달(= 백엔드 사설 IP)
    ports:
      - "80:80"                    # EC2 80 → 컨테이너 80 (사용자 접속 지점)
    logging:
      driver: json-file
      options:
        max-size: "10m"            # 로그 파일 최대 10MB
        max-file: "3"              # 최대 3개 보관
```

#### 프론트 `/opt/kiosk/.env`

```dotenv
# 백엔드 EC2 사설 IP. 공인이 아니라 같은 VPC 내부로만 통신(격리 유지)
BACKEND_HOST=172.31.9.140
```

> ⚠️ **헷갈리기 쉬운 3개의 IP 변수**
> - `BACKEND_PRIVATE_IP` (GitLab 변수): 매니저가 **배포하러 접속할** 백엔드 주소
> - `FRONTEND_PRIVATE_IP` (GitLab 변수): 매니저가 **배포하러 접속할** 프론트 주소
> - `BACKEND_HOST` (프론트 `.env`): nginx가 **API를 넘길** 백엔드 주소
> 값은 겹칠 수 있어도 **목적이 다릅니다.**

---

## 6. 파일들이 배포 단계에서 어떻게 이어지는가

### 백엔드 배포 체인

| 순서 | 파일·설정 | 다음으로 넘기는 것 |
|---:|---|---|
| 1 | GitHub 백엔드 소스 | `main` 커밋 |
| 2 | `.github/workflows/mirror-to-gitlab.yml` | GitLab 백엔드 `main` |
| 3 | GitLab `.gitlab-ci.yml` | 매니저의 백엔드 Runner Job |
| 4 | `BACKEND_PRIVATE_IP` + Runner SSH 키 | 백엔드 EC2 접속 |
| 5 | `rsync` | `/opt/kiosk/src-backend` 최신 소스 |
| 6 | `Dockerfile` + `pom.xml` + `src/**` | `kiosk-backend:latest` 이미지 |
| 7 | `docker-compose.backend.yml` + `.env` | 실행 중인 `kiosk-backend` 컨테이너 |
| 8 | RDS 보안그룹 + 엔드포인트 | `kiosk` DB 데이터 |

### 프론트 배포 체인

| 순서 | 파일·설정 | 다음으로 넘기는 것 |
|---:|---|---|
| 1 | GitHub 프론트 소스 | `main` 커밋 |
| 2 | `.github/workflows/mirror-to-gitlab.yml` | GitLab 프론트 `main` |
| 3 | GitLab `.gitlab-ci.yml` | 매니저의 프론트 Runner Job |
| 4 | `FRONTEND_PRIVATE_IP` + Runner SSH 키 | 프론트 EC2 접속 |
| 5 | `rsync` | `/opt/kiosk/src-frontend` 최신 소스 |
| 6 | `Dockerfile` + `package*.json` + Vue 소스 | `kiosk-frontend:latest` 이미지 |
| 7 | `docker-compose.frontend.yml` + `.env` | `BACKEND_HOST`가 담긴 nginx 컨테이너 |
| 8 | `nginx.conf` | 정적 화면 + API·업로드 프록시 + SPA fallback |
| 9 | 프론트 탄력적 IP + 보안그룹 + `icekiosk.shop` DNS | 사용자 HTTP 80 접속 |

---

## 7. 트러블슈팅 & 보안 체크리스트

### 자주 나는 증상

| 증상 | 원인 | 확인/해결 |
|---|---|---|
| 프론트 502 | nginx가 백엔드에 못 닿음 | `BACKEND_HOST`가 백엔드 사설 IP인지, 백엔드 8080 떴는지, backend SG 8080 소스가 frontend SG인지 |
| 백엔드 `Communications link failure` | RDS에 못 붙음 | `DB_HOST`가 RDS 엔드포인트인지, `kiosk-sg-rds` 3306 소스가 백엔드 SG인지, 같은 VPC인지 |
| 백엔드 로그 `Access denied` | 계정/권한 | `kiosk` 계정 GRANT 됐는지, 비밀번호 일치하는지 |
| 한글 깨짐 | 문자셋 | RDS 파라미터그룹 utf8mb4 적용+재부팅, 덤프 로드 시 `--default-character-set=utf8mb4` |
| 파이프라인 pending | 러너 태그/오프라인 | `tags: [manager]` 일치, Runner online, 매니저 EC2 켜져 있는지 |
| 배포는 됐는데 옛날 화면 | 브라우저 캐시 | `Ctrl+Shift+R` 강력 새로고침 |

### 보안 체크리스트

- [ ] 프론트·백엔드 22 소스 = `kiosk-sg-manager` / 매니저 22 소스 = 내 IP만
- [ ] 백엔드 8080 소스 = 프론트 SG / RDS 3306 소스 = 백엔드 SG
- [ ] RDS **퍼블릭 액세스 = 아니요**
- [ ] 백엔드는 DML 권한만 가진 `kiosk` 계정 사용(마스터 admin 직접 사용 X)
- [ ] `.env`(RDS 비번, `HQ_TOKEN_SECRET` 등)는 Git에 없음

---

## 8. 현재 방식의 한계와 개선점

### 장점
- 사용자는 프론트 80만 접근, 백엔드·RDS는 보안그룹으로 계층 격리
- 매니저 한 대에서 두 프로젝트 배포를 통제
- GitHub를 원본으로, GitLab Runner를 CI/CD로 활용
- 서버 `.env`·`uploads`는 재배포에 덮이지 않음
- 프론트/백엔드 독립 배포 가능

### 한계 (개선 여지)
- 파이프라인에서 **테스트를 돌리지 않음**(`-DskipTests`)
- 새 컨테이너 실패 시 **자동 롤백 없음**(이전 이미지가 prune으로 사라질 수 있음)
- 컨테이너 교체 순간 **짧은 중단** 가능
- 대상 EC2에서 직접 빌드 → 서버 CPU/메모리 사용
- 두 Runner가 매니저의 같은 계정·같은 SSH 키 공유 → 프로젝트 간 격리 약함
- `docker-compose`·`.env`가 Git에 없어 서버 소실 시 자동 복구 어려움
- AWS 인프라(VPC/RDS)가 콘솔 수동 설정 → 재생성 자동화 없음
- 현재 HTTP만 → **도메인 TLS/HTTPS 미구성** (icekiosk.shop이 https 아님)

### 다음 개선 우선순위
1. `docker-compose`·`.env.example`을 버전 관리(비밀값 제외)
2. 파이프라인에 백엔드 테스트 / 프론트 빌드 검사 추가
3. 이미지에 커밋 SHA 태그 → 실패 시 이전 태그로 롤백
4. `icekiosk.shop`에 HTTPS(Let's Encrypt 등) 적용
5. Flyway/Liquibase로 DB 스키마 변경 이력 관리
6. 장기적으로 Terraform/CloudFormation으로 인프라 코드화

---

> **최종 한 줄 요약**
> `GitHub push → GitLab mirror → Manager Runner → rsync/SSH → 대상 EC2 docker build → compose 재생성`
> 서버 역할을 매니저·프론트·백엔드·RDS로 쪼개고, 코드는 GitHub에서만 고치며, 나머지는 자동으로 흐릅니다.
