# A09 — 보안 로깅 및 모니터링 실패

> 발표 준비용 설명 문서. 보고서(`kiosk_보안_OWASP_2025_Top10.html`)의 A09 섹션을 말로 풀어 정리한 것.

## A09가 뭔가

**"공격이 일어났는데 기록에 안 남거나, 기록이 못 믿을 것이 되는" 문제.**

다른 항목들이 *공격을 막는* 것이라면, A09는 **공격을 기록하고 알아차리는** 쪽이다. 침입을 100% 막을 수는 없으니 최소한 **누가 언제 무엇을 했는지는 남아야** 한다.

로그가 무너지는 방식은 두 가지다.

1. **기록이 오염된다** → 로그를 못 믿게 됨 (①②번)
2. **기록이 아예 안 남는다** → 공격을 눈치채지 못함 (③번)

---

## ①② CRLF 로그 위조 — 기록이 오염되는 문제

### 로그의 대전제: 한 줄 = 한 사건

```
2026-08-26 10:00:01 WARN - 로그인 실패: id=hong
2026-08-26 10:00:02 WARN - 로그인 실패: id=kim
```

로그를 분석하는 도구도, 사람도 **줄 단위로** 읽는다. 그래서 **줄을 하나 늘릴 수 있으면 사건을 하나 지어낼 수 있다.**

### 공격 방법

`\r\n`은 줄바꿈 문자(엔터)다. 아이디 입력칸에 이걸 넣어서 로그인을 시도한다.

```bash
curl -X POST .../login -d '{"loginId":"test\r\n[INFO] 관리자 로그인 성공"}'
```

서버는 "로그인 실패했으니 아이디를 로그에 남기자"라고 **정상 동작**한다. 그런데 그 아이디 안에 줄바꿈이 있으니 결과는 이렇게 된다.

```
[WARN] 로그인 실패: id=test
[INFO] 관리자 로그인 성공          ← 서버가 쓴 적 없는 가짜 줄
```

**없던 기록이 생겼다.** 반대로 진짜 공격 흔적을 가짜 줄 수천 개로 파묻어 못 찾게 만들 수도 있다.

정식 명칭은 **CWE-117 로그 위조(Log Injection)**.

### ① 1층 방어 — logback 설정 (전역)

`backend/src/main/resources/logback-spring.xml` 에서 **로그가 찍히는 마지막 순간**에 줄바꿈을 공백으로 바꾼다.

```xml
<pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %replace(%m){'[\r\n]+', ' '}%n</pattern>
```

- `%m` = 로그 메시지
- `%replace(%m){'[\r\n]+', ' '}` = 그 안의 `\r` `\n` 을 공백으로 치환
- **콘솔용·파일용 두 군데 모두**에 적용되어 있다

**장점**: 코드를 한 줄도 안 고쳐도 **모든 로그**에 자동 적용된다.

### ② 2층 방어 — CrlfUtils (코드에서)

`backend/src/main/java/com/kiosk/global/security/CrlfUtils.java`

```java
public static String forLog(String value) {
    if (value == null) return null;
    return value.replace("\r", "").replace("\n", "");
}
```

로그를 남기는 코드에서 **미리** 정제한다.

```java
log.warn("로그인 실패: id={}", CrlfUtils.forLog(loginId));
```

### 왜 두 번 하나 — ②번의 핵심

1층만 있으면 **설정 파일 하나에 전부 의존**하게 된다. 누가 실수로 `logback-spring.xml`을 고치거나 다른 로깅 방식을 추가하면 그 순간 전부 뚫린다.

두 층은 **독립적으로** 동작한다. 하나가 실패해도 나머지가 막는다 — 이것을 **심층 방어(Defense in Depth)** 라고 한다.

> **발표 한 줄**: "설정은 언제든 바뀔 수 있으니, 코드에서도 한 번 더 막았습니다."

---

## ③ 무차별 대입 탐지 — 기록이 안 남는 문제

### 공격 방법

로그인 API에 `curl`로 비밀번호만 바꿔가며 **초당 수백 번** 던진다. 시간만 충분하면 언젠가 맞는다.

**더 큰 문제는 이것이 일어나도 서버 로그에 경고가 남지 않는다는 것.** 관리자는 공격받는 줄도 모른다.

### 조치 — LoginRateLimitFilter

`backend/src/main/java/com/kiosk/global/security/LoginRateLimitFilter.java`

같은 IP가 **5분에 10회**를 넘기면

- HTTP **429**(Too Many Requests) 응답 → 공격 자동 무력화
- 경고 로그를 **딱 한 번만** 기록

### 왜 "딱 한 번"이 중요한가

초당 수백 번 공격이 오는데 매번 로그를 남기면 **로그 파일이 폭주해서** 정작 다른 기록이 파묻힌다. 공격 자체가 **로그를 지우는 공격**이 되어버린다.

그래서 "차단이 시작되는 순간"에만 한 줄 남긴다 — **탐지는 되면서 로그는 안 터진다.**

### ①②와 연결되는 지점

이 필터가 남기는 로그에는 **IP 주소**가 들어간다. 그런데 그 IP는 `X-Forwarded-For` 헤더에서 복원한 값이라 **공격자가 조작할 수 있다.** 그래서 여기에도 `CrlfUtils`를 적용했다.

> **발표 한 줄**: "앞서 만든 CrlfUtils를 여기서도 씁니다. 방어가 서로 연결되는 거죠."

---

## 세 줄 요약

| # | 문제 | 조치 |
|---|---|---|
| ① | 입력값의 줄바꿈으로 **가짜 로그 줄** 생성 가능 | logback 설정에서 **전역** 치환 |
| ② | 설정이 바뀌면 1층 방어에 **구멍** | 코드에서도 `CrlfUtils`로 **한 번 더** |
| ③ | 비밀번호 자동 대입 공격이 **기록조차 안 됨** | 5분 10회 초과 시 **429 차단 + 경고 1회** |

**A09 전체를 관통하는 문장**

> 로그는 사고가 났을 때 유일한 증거입니다.
> 그 증거가 위조되거나 아예 없으면, 막는 것 자체가 의미가 없어집니다.

---

## 예상 질문

| 질문 | 답 |
|---|---|
| 왜 `\r\n`이 위험한가? | 로그는 **한 줄이 한 기록**이라, 줄바꿈을 넣으면 없던 기록을 지어낼 수 있다 |
| logback 설정만으로 충분하지 않나? | 설정 파일 하나에 의존하게 된다. 누가 고치거나 다른 로깅 방식이 추가되면 그 순간 뚫린다 |
| 왜 경고 로그를 한 번만 남기나? | 매번 남기면 공격 트래픽이 로그를 폭주시켜, 공격이 곧 **로그를 지우는 공격**이 된다 |
| 5분 10회는 근거가 있나? | 매장 공유기 뒤에 키오스크 여러 대가 물릴 수 있어, 계정이 아니라 IP 기준이라 임계치를 넉넉히 잡았다 |
